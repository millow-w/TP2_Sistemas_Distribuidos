apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-2
  template:
    metadata:
      labels:
        app: client-2
    spec:
      containers:
      - name: client
        image: camilaapa/tp2-cluster-sync-redis:latest
        imagePullPolicy: Never
        env:
        - name: CLIENT_ID
          value: "client-2"
        - name: NODE_URL
          value: "http://node-2.node:5000"
        command: ["python", "client.py"]